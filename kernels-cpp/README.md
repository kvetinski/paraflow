# Native kernels

C++ code begins in Week 2 when ParaFlow has a scalar oracle against which SIMD
work can be tested.

Planned backends:

- scalar C++ comparison;
- explicit SIMD;
- ISPC;
- CUDA.

Day 1 does not create a placeholder library or claim native-kernel support.
